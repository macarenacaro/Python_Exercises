"""Ejercicio1: 
Descompón el ejercicio 3 de esta sesión anterior en módulos, de forma que cada clase esté en un módulo, y mediante
 instrucciones import se incluyan donde se necesiten. Utiliza un IDE que te permita gestionar cómodamente estos archivos."""

"""
Ejercicio2:
Haz un programa que le pida al usuario un nombre de imagen, la cargue y, si existe, le pida continuamente tamaños 
a los que la quiera escalar (ancho y alto), y guarde una copia de la imagen original con ese tamaño. 
El nombre de cada imagen escalada tendrá el patrón ancho_alto_nombreFicheroOriginal. El proceso 
terminará cuando el usuario ponga la anchura o altura a 0.

Pista
Para saber si un archivo existe o no puedes usar el módulo os.path del núcleo de Python, y su método isfile:
import os.path
if (os.path.isfile(nombreFichero)):
..."""